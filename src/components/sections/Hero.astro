---
import { Image } from 'astro:assets';
import { type HeroProps } from '../../types/components';
import logoImage from '../../assets/logo.png';

interface Props extends HeroProps {}
const {
  headline = "Technical Due Diligence That Defends Itself — for Utility-Scale BESS",
  subheadline = "Structured technical analysis adapted to your mandate — investment screening, lender review, or independent assessment. Every finding documented. Every conclusion traceable.",
  ctaText = "Request Initial Scope",
  ctaHref = "#contact"
} = Astro.props;
---

<section class="bg-white relative overflow-hidden">
  <div class="section-container relative z-10 pt-12 pb-16 lg:pt-24 lg:pb-28">
    <div class="max-w-3xl">
      <Image src={logoImage} alt="BiP Omega" class="h-28 lg:h-40 w-auto mb-8 lg:mb-12" />
      <h1 class="text-3xl lg:text-5xl font-bold text-heading leading-tight mb-6">
        {headline}
      </h1>
      <p class="text-xl text-body mb-8 leading-relaxed">
        {subheadline}
        <br/><br/>
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start items-start">
        <a href={ctaHref} class="btn-primary text-center">
          {ctaText}
        </a>
        <span class="text-sm text-slate-500 self-center">Initial scope assessment: typically within 48 hours</span>
      </div>
    </div>
  </div>
  
  <!-- Subtle institutional pattern/accent can go here -->
  <div class="absolute top-0 right-0 -z-10 w-1/3 h-full bg-bg-light opacity-50 skew-x-12 translate-x-20"></div>
</section>
